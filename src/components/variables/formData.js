const formData = {
    polish: "",
    english: "",
    wordStatus: {
      memorize: false,
      repeated: false,
      timesRepeated: 0,
    }
}

const { english, wordStatus} = formData

export default formData;
export { english, wordStatus }